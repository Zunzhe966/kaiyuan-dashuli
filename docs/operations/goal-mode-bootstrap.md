# 目标模式启动文本

以下正文用于启动长期任务；完整规则见同目录 `long-running-goal-mode.md`。

```text
持续完成“开源大梳理”，不要停在计划、解释或状态汇报。仓库为 Zunzhe966/kai-yuan-da-shu-li，生产分支 main，网站为 https://kai-yuan-da-shu-li.pages.dev。每次启动先读取 AGENTS.md、docs/operations/long-running-goal-mode.md、V2 设计、当前实施计划，再核对工作区、origin/main、未提交 worktree、PR、最近 verify/pages-deploy 和线上 /api/v1/meta.json；以实际证据恢复断点。

唯一目标：建立面向人类和智能体的全球开源项目发现与选型系统。用户应能从领域、子领域、任务、能力和项目形态开始，再按技术、部署、平台、中文/英文支持、开源性质、许可证义务、具体版本、维护、安全和成熟度逐层筛选，得到具体项目与具体版本、匹配理由、未知项和证据。网站默认中文并可切英文；网站、HTTP、MCP 和离线数据必须使用同一 Schema 和查询语义。

采用两层模型：程序持续维护全球发现层，保存所有可访问公开仓库的基础元数据、来源、更新时间、质量级别、增量和游标；高价值候选经深度核验后进入精选选型图谱。不得把未经核验的发现记录称为推荐，不得在本机 clone 或执行外部仓库，不得为上亿仓库在 GitHub/Pages 生成一仓一文件。海量数据用可校验分片、对象存储和查询索引；GitHub保存程序、Schema、精选图谱、清单、游标与校验和。

深度核验每个仓库时检查：稳定平台 ID、规范 URL/改名/转移/镜像；中英文摘要及 use/avoid；领域/任务/能力/形态；编程语言/框架/运行时/协议/数据类型；本地/自托管/云/容器/Kubernetes/桌面/移动部署；系统和硬件平台；中文/英文界面、文档、社区；open-source/open-core/source-available/未知；按版本生效的 SPDX/LicenseRef、商用/修改/分发/署名/源码公开/网络公开源码/专利义务；稳定/LTS/预发布/EOL；维护、安全、成熟度、限制、替代/依赖/集成/后继/生态关系；每项关键事实的直接证据、观察时间、适用版本和状态。未知绝不默认满足，宣传语、star 和模型推断不能当已核验事实。

候选先入 quarantine；按 platform + platform_repository_id 去重；通过双语、版本许可证、证据、关系、Schema 和检索闸门后才提升。筛选语义固定为跨维度 AND、同维度多选默认 OR，支持 must/should/must_not；should 只重排；unknown_policy 显式处理未知；结果必须指出匹配版本和证据。禁止继续用项目 ID 硬编码检索特判。

生产链路固定为精选源数据和关系 → PR → verify → 受保护 main → main verify → pages-deploy 重建 build/site → Wrangler 上传 Cloudflare Pages → 线上提交/目录哈希/节点/边/关键页面核验。GitHub main 是唯一生产事实源；Cloudflare不是数据库、不是双向同步；浏览器和手动上传不是日常发布方式。

主模型是唯一负责人、默认写入者和最终验收者；最多一个子代理且不得再派生。子代理只做边界明确的研究、抽样、测试、失败分析或审查；需要写入则使用隔离 worktree；二者不得同时修改同一文件或分支。采集程序并行不受两个模型槽限制，但必须限流、退避、幂等、断点续跑和失败隔离。

使用 approval_policy=never、workspace-write、network_access=true；在既有权限内持续工作，越界动作失败后记账并改做其他任务，不请求提升权限。不得购买域名、付款、注册收费服务、提交银行税务材料、扩大第三方授权、删除生产资源或公开秘密。Token 只能进入平台 Secret，不得输出或提交。

执行顺序：1 审查拆分遗留快照修复并统一源数据/dist/站点；2 恢复并验证 main 到 Pages 自动发布并加入提交/目录哈希；3 测试先行实现 Schema V2 与迁移器；4 迁移现有精选项目；5 统一网站/HTTP/MCP 查询；6 建立发现层增量闭环并逐步扩展；7 稳定且有真实价值后再处理域名、推广和广告。

每完成一个原子批次立即运行验证、形成独立提交、记录实际结果和下一断点，然后继续。代码已写、PR已建、测试未完或线上未更新都不能称为完成。遇到外部阻塞时继续最靠前的不受阻任务，绝不虚报覆盖或完成。
```
